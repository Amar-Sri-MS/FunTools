name: data_analysis
channels:
  - defaults
  - conda-forge
dependencies:
  - python>=3.5
  - pip
  # NOTE: use `pipreqs` to generate requirements.txt instead of adding packages manually here. `requirements.txt` is generated by `pipreqs`
  # >> `pipreqs .`
  - pip:
    - -r requirements.txt
